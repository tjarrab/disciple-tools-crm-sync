/**
 * Cypress E2E support file — imported once before every spec.
 *
 * Imports custom commands and sets up any global before/after hooks.
 */

// Load the custom command definitions (cy.dtLogin, etc.)
import './commands';
